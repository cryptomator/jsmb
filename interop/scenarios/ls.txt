# Lists the share root and a freshly-created subdirectory.
# Requires milestones: M4 (TREE_CONNECT), M5 (CREATE / CLOSE), M6 (QUERY_DIRECTORY).
# Until M6 lands, the `ls` lines will return NT_STATUS_NOT_SUPPORTED — that's expected; kept here as
# a forward-looking regression scenario for when directory enumeration is implemented.
ls
mkdir ls-scenario
cd ls-scenario
ls
cd \
exit
